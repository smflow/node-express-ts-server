export const config = {
  defaultPort: 9823, // use this port if no PORT found in .env file
  prefix: "/", // url prefix like: {prefix}/path
  bodyParserLimit: "10mb" // limit for body parser to limit the incoming data
};